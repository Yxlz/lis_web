@CHARSET "UTF-8";
*{
    margin: 0;
    padding: 0;
    -webkit-box-sizing: border-box;
       -moz-box-sizing: border-box;
            box-sizing: border-box;
}
/* 两列  左侧固定  右侧自适应布局 */
.left-fixed_right-auto{
    width: 100%;
    clear: both;
    display: inline-block;
    overflow-x : hidden; 
    margin-top:45px;
}
/* 左边部局 */
.left{
    position:fixed;
    /* float:left; */
    left:0;
    right:80%;
    width:20%;/* 数值核心1 */
    height: 100%;
    background: #708090;
    overflow-y:auto; 
    /* margin-right:-25%; *//* 数值核心2 */
}
/* 左边菜单位置控制 */
.leftMainMenu{
	position:absolute;
	top: 0;
	left:5px;
	right:5px;
	bottom: 10px;
}
/* 主页面容器 */
.container{
    float:right;
    width:80%;
    height: 100%;
    padding-left: 5px !important;
    padding-right:5px !important;
    
}
.tabContainer{
    float:right;
    width:80%;
    height: 100%;
    padding-left: 0px !important;
    padding-right:0px !important;
    padding-bottom:0px !important;
}
/* 顶部导航条 */
.topbar{
	width:100%;/* 数值核心1 */
    height: 45px;
    background-image: url(../img/head_item.png);
    position:fixed;
    top:0px;
    /* z-index:9; */
}
/* 控制主菜单大小 */
.list-group-item{
	padding: 7px 5px !important;
}
/* 面板设置 */
.panel {
	margin-bottom: 0px;
}
.panel-body {
	margin: 0px;
	padding: 0px;
}
/* tabs */
#myTab {
	padding-left: 5px;
	background:#708090;
}
.nth-tabs{
    width: 80%;
    position: relative;
   /*  height: 42px; */
    background: #fafafa;
   /*  line-height: 40px; */
    overflow: hidden;
    left:20%;
    /* z-index: 9; */
}
.tab-dropdown-menu {
    position: fixed;
    top: 13%;
    left: 86%;
    /* z-index: 9; */
    display: none;
    padding: 5px 0;
    margin: 2px 0 0;
    font-size: 14px;
    text-align: left;
    list-style: none;
    background-color: #fff;
    -webkit-background-clip: padding-box;
    background-clip: padding-box;
    border: 1px solid #ccc;
    border: 1px solid rgba(0,0,0,.15);
    border-radius: 4px;
    -webkit-box-shadow: 0 6px 12px rgba(0,0,0,.175);
    box-shadow: 0 6px 12px rgba(0,0,0,.175);
}
/* navbar */
.navbar {
	min-height:0px
}
/* navbar */
.navbar-inverse {
	margin-bottom: 1px;
	height: 45px;
}
.navbar-nav>li>a {
    padding-top: 15px;
    padding-bottom: 15px;
    padding-left: 20px;
    padding-right: 30px;
}

/* ul>li>a{
	background: #F5FFFA;
} */
.tab-pane {
	background: white;
	height: 100%;
}
/* footer */
.footer {
	background-color: #000000;
	width: 80%;
	height: 25px; /* footer的高度一定要是固定值*/
	position: fixed;
	bottom: 0px;
	left: 20%;
}

#copyright {
	line-height: 18px;
	text-align: center;
	font-size: 12px;
	color: #ffffff;
}

.btn-noborder {
	border-style: none;
	outline: none;
}

.modal-dialog {
	width: 400px;
}

.fixed-table-toolbar .bs-bars,.fixed-table-toolbar .columns,.fixed-table-toolbar .search
{
	margin-top: 4px !important;
	margin-bottom: 4px !important;
}
iframe{
	height:550px;
}