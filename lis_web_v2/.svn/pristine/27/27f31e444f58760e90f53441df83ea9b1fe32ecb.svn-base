/*
 * 应用程序入口文件 
 */

Ext.ns("App");

App.init = function() {
	Ext.QuickTips.init();
	//开启动态加载
    //Ext.Loader.setConfig({
      //  enabled: true
    //});
	Ext.Msg.minWidth = 300;
	App.LoginWin.show();
};

Ext.onReady(App.init);